# testing-practice-repo
This Repo for practicing purpose

This repo we are practicing for terraform

Day 1 - Git Commands

--> git clone <https url of git repo>
--> git status
--> git add . <include all the files to staged>
--> git restore --stage <file name> --> revert staged file to unstage status
--> git commit -m "save all the files to keep ready to push"
--> git push < push local changes to remote repository>
